{
  "name": "based-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro build && wrangler dev",
    "deploy": "astro build && wrangler deploy",
    "astro": "astro",
    "format": "oxfmt --write .",
    "lint": "oxlint .",
    "lint-fix": "oxlint --fix .",
    "check": "astro check"
  },
  "dependencies": {
    "astro": "^6.4.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@types/node": "^25.9.1",
    "oxfmt": "^0.52.0",
    "oxlint": "^1.67.0",
    "typescript": "^6.0.3",
    "wrangler": "^4.95.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
