{
  "name": "based-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Based - The Everything Database App (Monorepo)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter desktop dev",
    "build": "bun --filter desktop run build",
    "preview": "bun --filter desktop run preview",
    "tauri": "bun --filter desktop tauri",
    "lint": "bun --filter desktop run lint",
    "format": "bun --filter desktop run format",
    "type-check": "bun --filter desktop run type-check"
  }
}
