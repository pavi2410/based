# Based documentation

## Project format (canonical)

**[The `.based` project format](./based-project/README.md)** — specification for `project.toml`, connections, queries, local state, and schema versioning. This is the source of truth for how `.based/` should work for users and implementers.

## P0 engineering specs

Implementation-facing product and track docs live under [p0/](./p0/README.md). Where P0 docs conflict with [based-project/](./based-project/README.md), the project format spec wins for `.based/` layout and file semantics.
