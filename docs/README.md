# Based documentation

## Project format (canonical)

**[The `.based` project format](./based-project/README.md)** — specification for `project.toml`, connections, queries, local state, and schema versioning. This is the source of truth for how `.based/` should work for users and implementers.
