# Copy to .env and fill in values for local connections.

# Local PostgreSQL (docker compose — default password is "based")
LOCAL_PG_PASSWORD=based

# Local MongoDB (docker compose)
MONGO_URL=mongodb://localhost:27017
