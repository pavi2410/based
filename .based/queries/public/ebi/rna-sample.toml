schema_version = 1

name = "RNA Sample"
description = "Recent RNA sequences with URS identifiers"
tags = ["rna", "explore"]

[target]
connection = "public/ebi"

[sql]
query = """
SELECT id, upi, taxid, len, timestamp
FROM rna
ORDER BY timestamp DESC NULLS LAST
LIMIT 25;
"""
