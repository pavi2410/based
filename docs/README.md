# Based documentation

## Product docs (website)

User-facing guides live on the site under [`/docs`](https://based.pavi2410.com/docs): databases, connections, editor, table grid, UI, navigation, settings, and configuration.

## Project format (canonical)

**[The `.based` project format](./based-project/README.md)** — specification for `project.toml`, connections, queries, local state, and schema versioning. This is the source of truth for how `.based/` should work for implementers.
