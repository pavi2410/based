schema_version = 1

name = "Base Collection Sample"
description = "First documents from the bootstrap base collection"
tags = ["explore"]

[target]
connection = "public/watertap"

[aggregate]
collection = "base"
pipeline = """
[
  { "$limit": 10 }
]
"""
